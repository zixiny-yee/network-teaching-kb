TCP/IP协议是Internet的核心协议，分为四层：应用层、传输层、网络层和网络接口层。应用层协议包括HTTP（网页浏览）、FTP（文件传输）、SMTP（邮件发送）等。传输层有TCP和UDP两种协议，TCP面向连接、可靠传输，UDP无连接、效率高。网络层的IP协议负责数据包的寻址和路由，每个设备都有唯一的IP地址。IP地址分为IPv4（如192.168.1.1）和IPv6两种。DNS域名系统将域名转换为IP地址，方便用户记忆和使用。